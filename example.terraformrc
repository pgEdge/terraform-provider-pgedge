// paste this file in $HOME/ to test the provider locally with terraform cli
// Note: Only dev_overrides during local testing

provider_installation {
  dev_overrides {
    "pgEdge/pgedge" = "${HOME}/go/bin" // path to go bin
  }
  direct {}
}